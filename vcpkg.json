{
  "name": "graticule",
  "version-string": "0.1.0",
  "description": "Dependencies for building the Graticule FFGL plugin. GLEW is only needed on Windows; macOS uses the system OpenGL framework. There are no other dependencies: the font is a table in Font.cpp and the colour maths is arithmetic.",
  "dependencies": [
    {
      "name": "glew",
      "platform": "!osx"
    }
  ]
}
